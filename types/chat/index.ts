export type ChatHistory = {
    id: string;
  
  question  : string;
  answer:string

  };


 export  type MessageBubbleProps = {
    message: string;
    isUser: boolean;
  };

  export type Chat={
    question:string
  }